const types = { item: 'item', group: 'group', collapse: 'collapse' }

const apiKey = {
  id: 'id-api-key',
  title: 'API Key',
  // caption: 'API key',
  type: types.group,
  // children: [...colections],
};


export default apiKey