package com.finaltest.selenium.pages.flookpages;

import org.openqa.selenium.WebDriver;

public class ChatStoryPage extends BasePage {
    public ChatStoryPage(WebDriver driver) {
        super(driver);
    }
}
